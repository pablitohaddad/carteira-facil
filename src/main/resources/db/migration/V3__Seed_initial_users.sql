-- Migration removed: initial seeding disabled.
-- NOTE: This file previously inserted plain-text passwords. Seeding is now
-- performed by Java initializer (DataInitializer) which hashes passwords.
-- To re-enable SQL seeding, recreate this migration with hashed passwords.

